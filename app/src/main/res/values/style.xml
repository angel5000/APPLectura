<?xml version="1.0" encoding="utf-8"?>

    <resources>
        <!-- Tema principal sin ActionBar -->
        <style name="Theme.AppLectura.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
            <item name="colorPrimary">@color/purple_500</item>
            <item name="colorPrimaryDark">@color/purple_700</item>
            <item name="colorAccent">@color/teal_200</item>
            <item name="android:windowSoftInputMode">adjustResize</item>

        </style>
    <style name="MenuItemTitleStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#FFFFFF</item> <!-- Cambia el color a tu preferencia -->
        <item name="android:textSize">24sp</item> <!-- Opcional: ajusta el tamaño -->
    </style>
    </resources>
